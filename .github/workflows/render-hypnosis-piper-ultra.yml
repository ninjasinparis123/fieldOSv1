name: Render Piper Ultra Sample

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/render-hypnosis-piper-ultra.yml'
      - 'tools/render_piper_ultra.sh'
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Piper and German voice
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          python -m pip install --upgrade pip
          pip install piper-tts
          python -m piper.download_voices de_DE-thorsten-high
      - name: Render 20 second neural sample
        run: bash tools/render_piper_ultra.sh
      - name: Upload MP3
        uses: actions/upload-artifact@v4
        with:
          name: hypnosis-human-voice-20s
          path: hypnosis_build/Hypnose_MENSCHLICHE_STIMME_20s.mp3
          if-no-files-found: error
          retention-days: 7
